# CPU-only torch/torchvision (macOS users: drop the +cpu suffix, no +cpu wheels for macOS)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.2+cpu
torchvision==0.17.2+cpu

ultralytics==8.2.31
rapidocr==3.0.0
onnxruntime==1.18.1
opencv-python-headless==4.9.0.80
pandas==2.2.2
numpy==1.26.4
gdown==5.2.0

# Required to execute the grading notebook. Do not remove.
nbconvert==7.16.4
ipykernel==6.29.4
